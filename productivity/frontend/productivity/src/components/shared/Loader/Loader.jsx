import {Icon} from '@iconify/react';

function Loader({icon,className}){
    return(
        <Icon 
            icon = {icon}
            className = {className}
        />
    )
}

export default Loader;