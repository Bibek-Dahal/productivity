function Dashboard({className}){
    return(
        <div
            className = {`${className ? className : ""}`}
        >
            <p>
                dashboard
            </p>
        </div>
    )
}

export default Dashboard;